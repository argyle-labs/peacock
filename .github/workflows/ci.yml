name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: argyle-labs/.github/.github/workflows/plugin-ci.yml@feat/plugin-ci-container
    secrets: inherit
