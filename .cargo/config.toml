# Development override: resolve the orca git deps to the local in-tree crates
# so this repo builds against an unreleased plugin-toolkit alongside a
# checked-out orca repo at ../orca. Without this patch, the orca crates resolve
# from the pinned git branch in Cargo.toml.
#
# Use the git CLI (which inherits the operator's authenticated gh / ssh
# credentials) so cargo can resolve the git source that the `[patch]` below
# redirects to a local path.
[net]
git-fetch-with-cli = true

[patch."https://github.com/argyle-labs/orca"]
plugin-toolkit = { path = "../orca/projects/plugin-toolkit" }
contract = { path = "../orca/projects/contract" }
derive = { path = "../orca/projects/derive" }
