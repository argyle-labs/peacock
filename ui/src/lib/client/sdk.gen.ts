// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type {
  AgentGetData,
  AgentGetErrors,
  AgentGetResponses,
  AgentListData,
  AgentListErrors,
  AgentListResponses,
  AgentRunData,
  AgentRunErrors,
  AgentRunResponses,
  AuthChangePasswordData,
  AuthChangePasswordErrors,
  AuthChangePasswordResponses,
  AuthLoginData,
  AuthLoginErrors,
  AuthLoginResponses,
  AuthLogoutData,
  AuthLogoutErrors,
  AuthLogoutResponses,
  AuthMeData,
  AuthMeErrors,
  AuthMeResponses,
  AuthSessionCreateData,
  AuthSessionCreateErrors,
  AuthSessionCreateResponses,
  AuthSessionDeleteData,
  AuthSessionDeleteErrors,
  AuthSessionDeleteResponses,
  AuthSessionDetailData,
  AuthSessionDetailErrors,
  AuthSessionDetailResponses,
  AuthSigninData,
  AuthSigninErrors,
  AuthSigninResponses,
  AuthSignoutData,
  AuthSignoutResponses,
  AuthSignupData,
  AuthSignupErrors,
  AuthSignupResponses,
  AuthSignupStatusData,
  AuthSignupStatusResponses,
  AuthTokenCreateData,
  AuthTokenCreateErrors,
  AuthTokenCreateResponses,
  AuthTokenDeleteData,
  AuthTokenDeleteErrors,
  AuthTokenDeleteResponses,
  AuthTokenListData,
  AuthTokenListErrors,
  AuthTokenListResponses,
  ConfigDeleteData,
  ConfigDeleteErrors,
  ConfigDeleteResponses,
  ConfigDetailData,
  ConfigDetailErrors,
  ConfigDetailResponses,
  ConfigListData,
  ConfigListErrors,
  ConfigListResponses,
  ConfigUpsertData,
  ConfigUpsertErrors,
  ConfigUpsertResponses,
  ContainersExecData,
  ContainersExecErrors,
  ContainersExecResponses,
  ContainersListData,
  ContainersListErrors,
  ContainersListResponses,
  ContainersLogsData,
  ContainersLogsErrors,
  ContainersLogsResponses,
  ContainersReconcileData,
  ContainersReconcileDryData,
  ContainersReconcileDryErrors,
  ContainersReconcileDryResponses,
  ContainersReconcileErrors,
  ContainersReconcileResponses,
  ContainersUnholdData,
  ContainersUnholdErrors,
  ContainersUnholdResponses,
  ContainersUnwedgeData,
  ContainersUnwedgeErrors,
  ContainersUnwedgeResponses,
  DbCompactData,
  DbCompactErrors,
  DbCompactResponses,
  DbDetailData,
  DbDetailErrors,
  DbDetailResponses,
  DbStatsData,
  DbStatsErrors,
  DbStatsResponses,
  DbSweepData,
  DbSweepErrors,
  DbSweepResponses,
  DbUpdateData,
  DbUpdateErrors,
  DbUpdateResponses,
  FilesDeleteData,
  FilesDeleteErrors,
  FilesDeleteResponses,
  FilesListData,
  FilesListErrors,
  FilesListResponses,
  FilesReadData,
  FilesReadErrors,
  FilesReadResponses,
  FilesSearchData,
  FilesSearchErrors,
  FilesSearchResponses,
  FilesStatData,
  FilesStatErrors,
  FilesStatResponses,
  FilesTreeData,
  FilesTreeErrors,
  FilesTreeResponses,
  FilesUpdateData,
  FilesUpdateErrors,
  FilesUpdateResponses,
  InventoryTreeData,
  InventoryTreeErrors,
  InventoryTreeResponses,
  LxcDeleteData,
  LxcDeleteErrors,
  LxcDeleteResponses,
  LxcDetailData,
  LxcDetailErrors,
  LxcDetailResponses,
  LxcListData,
  LxcListErrors,
  LxcListResponses,
  LxcProvisionData,
  LxcProvisionErrors,
  LxcProvisionResponses,
  LxcRebootData,
  LxcRebootErrors,
  LxcRebootResponses,
  LxcShutdownData,
  LxcShutdownErrors,
  LxcShutdownResponses,
  LxcStartData,
  LxcStartErrors,
  LxcStartResponses,
  LxcStopData,
  LxcStopErrors,
  LxcStopResponses,
  ModelBackendsCheckData,
  ModelBackendsCheckErrors,
  ModelBackendsCheckResponses,
  ModelCreateData,
  ModelCreateErrors,
  ModelCreateResponses,
  ModelDeleteData,
  ModelDeleteErrors,
  ModelDeleteResponses,
  ModelDetailData,
  ModelDetailErrors,
  ModelDetailResponses,
  ModelListData,
  ModelListErrors,
  ModelListResponses,
  ModelUpdateData,
  ModelUpdateErrors,
  ModelUpdateResponses,
  NamespaceAccessCreateData,
  NamespaceAccessCreateErrors,
  NamespaceAccessCreateResponses,
  NamespaceAccessDeleteData,
  NamespaceAccessDeleteErrors,
  NamespaceAccessDeleteResponses,
  NamespaceAccessListData,
  NamespaceAccessListErrors,
  NamespaceAccessListResponses,
  NamespaceCreateData,
  NamespaceCreateErrors,
  NamespaceCreateResponses,
  NamespaceDeleteData,
  NamespaceDeleteErrors,
  NamespaceDeleteResponses,
  NamespaceDetailData,
  NamespaceDetailErrors,
  NamespaceDetailResponses,
  NamespaceListData,
  NamespaceListErrors,
  NamespaceListResponses,
  NamespaceUseData,
  NamespaceUseErrors,
  NamespaceUseResponses,
  NetworkTopologyViewData,
  NetworkTopologyViewErrors,
  NetworkTopologyViewResponses,
  NotifySendData,
  NotifySendErrors,
  NotifySendResponses,
  PkiCreateData,
  PkiCreateErrors,
  PkiCreateResponses,
  PkiListData,
  PkiListErrors,
  PkiListResponses,
  PluginCreateData,
  PluginCreateErrors,
  PluginCreateResponses,
  PluginDeleteData,
  PluginDeleteErrors,
  PluginDeleteResponses,
  PluginDetailData,
  PluginDetailErrors,
  PluginDetailResponses,
  PluginInstallData,
  PluginInstallErrors,
  PluginInstallResponses,
  PluginListData,
  PluginListErrors,
  PluginListResponses,
  PluginUninstallData,
  PluginUninstallErrors,
  PluginUninstallResponses,
  PluginUpdateData,
  PluginUpdateErrors,
  PluginUpdateResponses,
  PodCancelOfferData,
  PodCancelOfferErrors,
  PodCancelOfferResponses,
  PodDetailData,
  PodDetailErrors,
  PodDetailResponses,
  PodForgetData,
  PodForgetErrors,
  PodForgetResponses,
  PodHistoryData,
  PodHistoryErrors,
  PodHistoryResponses,
  PodInstancesData,
  PodInstancesErrors,
  PodInstancesResponses,
  PodJoinData,
  PodJoinErrors,
  PodJoinResponses,
  PodKickData,
  PodKickErrors,
  PodKickResponses,
  PodLeaveData,
  PodLeaveErrors,
  PodLeaveResponses,
  PodListData,
  PodListErrors,
  PodListResponses,
  PodPingData,
  PodPingErrors,
  PodPingResponses,
  PodRecoverData,
  PodRecoverErrors,
  PodRecoverResponses,
  PodSnapshotData,
  PodSnapshotErrors,
  PodSnapshotResponses,
  PodSyncData,
  PodSyncErrors,
  PodSyncResponses,
  PodTrustData,
  PodTrustErrors,
  PodTrustResponses,
  PodUpdateData,
  PodUpdateErrors,
  PodUpdateResponses,
  ScheduleListData,
  ScheduleListErrors,
  ScheduleListResponses,
  ScheduleRunData,
  ScheduleRunErrors,
  ScheduleRunResponses,
  ScheduleStatusData,
  ScheduleStatusErrors,
  ScheduleStatusResponses,
  SchemaCreateData,
  SchemaCreateErrors,
  SchemaCreateResponses,
  SchemaDeleteData,
  SchemaDeleteErrors,
  SchemaDeleteResponses,
  SchemaDetailData,
  SchemaDetailErrors,
  SchemaDetailResponses,
  SchemaListData,
  SchemaListErrors,
  SchemaListResponses,
  SecretsCreateData,
  SecretsCreateErrors,
  SecretsCreateResponses,
  SecretsDeleteData,
  SecretsDeleteErrors,
  SecretsDeleteResponses,
  SecretsDetailData,
  SecretsDetailErrors,
  SecretsDetailResponses,
  SecretsListData,
  SecretsListErrors,
  SecretsListResponses,
  SecretsUpdateData,
  SecretsUpdateErrors,
  SecretsUpdateResponses,
  SecretsUpsertData,
  SecretsUpsertErrors,
  SecretsUpsertResponses,
  ServiceBackupData,
  ServiceBackupErrors,
  ServiceBackupResponses,
  ServiceConfigureData,
  ServiceConfigureErrors,
  ServiceConfigureResponses,
  ServiceDeployData,
  ServiceDeployErrors,
  ServiceDeployResponses,
  ServiceListData,
  ServiceListErrors,
  ServiceListResponses,
  ServiceRestoreData,
  ServiceRestoreErrors,
  ServiceRestoreResponses,
  ServiceStatusData,
  ServiceStatusErrors,
  ServiceStatusResponses,
  SpecCreateData,
  SpecCreateErrors,
  SpecCreateResponses,
  SpecDeleteData,
  SpecDeleteErrors,
  SpecDeleteResponses,
  SpecDetailData,
  SpecDetailErrors,
  SpecDetailResponses,
  SpecGraphqlDetailData,
  SpecGraphqlDetailErrors,
  SpecGraphqlDetailResponses,
  SpecGraphqlUpdateData,
  SpecGraphqlUpdateErrors,
  SpecGraphqlUpdateResponses,
  SpecListData,
  SpecListErrors,
  SpecListResponses,
  SpecRefreshData,
  SpecRefreshErrors,
  SpecRefreshResponses,
  SpecSyncMcpData,
  SpecSyncMcpErrors,
  SpecSyncMcpResponses,
  StorageListData,
  StorageListErrors,
  StorageListResponses,
  StorageMountCreateData,
  StorageMountCreateErrors,
  StorageMountCreateResponses,
  StorageMountData,
  StorageMountDeleteData,
  StorageMountDeleteErrors,
  StorageMountDeleteResponses,
  StorageMountDetailData,
  StorageMountDetailErrors,
  StorageMountDetailResponses,
  StorageMountErrors,
  StorageMountListData,
  StorageMountListErrors,
  StorageMountListResponses,
  StorageMountResponses,
  StorageMountUpdateData,
  StorageMountUpdateErrors,
  StorageMountUpdateResponses,
  StorageRecoverData,
  StorageRecoverErrors,
  StorageRecoverResponses,
  StorageSharesData,
  StorageSharesErrors,
  StorageSharesResponses,
  StorageUnmountData,
  StorageUnmountErrors,
  StorageUnmountResponses,
  SystemBuildData,
  SystemBuildErrors,
  SystemBuildResponses,
  SystemCapabilityDisableData,
  SystemCapabilityDisableErrors,
  SystemCapabilityDisableResponses,
  SystemCapabilityEnableData,
  SystemCapabilityEnableErrors,
  SystemCapabilityEnableResponses,
  SystemCapabilityListData,
  SystemCapabilityListErrors,
  SystemCapabilityListResponses,
  SystemCapabilityRecheckData,
  SystemCapabilityRecheckErrors,
  SystemCapabilityRecheckResponses,
  SystemDeleteData,
  SystemDeleteErrors,
  SystemDeleteResponses,
  SystemDetailData,
  SystemDetailErrors,
  SystemDetailResponses,
  SystemDetailViewData,
  SystemDetailViewErrors,
  SystemDetailViewResponses,
  SystemInstallData,
  SystemInstallErrors,
  SystemInstallResponses,
  SystemKillData,
  SystemKillErrors,
  SystemKillResponses,
  SystemRetentionGetData,
  SystemRetentionGetErrors,
  SystemRetentionGetResponses,
  SystemRetentionListData,
  SystemRetentionListErrors,
  SystemRetentionListResponses,
  SystemRetentionSetData,
  SystemRetentionSetErrors,
  SystemRetentionSetResponses,
  SystemServeReleaseData,
  SystemServeReleaseErrors,
  SystemServeReleaseResponses,
  SystemUpdateData,
  SystemUpdateErrors,
  SystemUpdateResponses,
  VmDeleteData,
  VmDeleteErrors,
  VmDeleteResponses,
  VmDetailData,
  VmDetailErrors,
  VmDetailResponses,
  VmListData,
  VmListErrors,
  VmListResponses,
  VmProvisionData,
  VmProvisionErrors,
  VmProvisionResponses,
  VmRebootData,
  VmRebootErrors,
  VmRebootResponses,
  VmShutdownData,
  VmShutdownErrors,
  VmShutdownResponses,
  VmStartData,
  VmStartErrors,
  VmStartResponses,
  VmStopData,
  VmStopErrors,
  VmStopResponses,
} from './types.gen';

export type Options<
  TData extends TDataShape = TDataShape,
  ThrowOnError extends boolean = boolean,
  TResponse = unknown,
> = Options2<TData, ThrowOnError, TResponse> & {
  /**
   * You can provide a client instance returned by `createClient()` instead of
   * individual options. This might be also useful if you want to implement a
   * custom client.
   */
  client?: Client;
  /**
   * You can pass arbitrary values through the `meta` object. This can be
   * used to access values that aren't defined as part of the SDK function.
   */
  meta?: Record<string, unknown>;
};

export const authChangePassword = <ThrowOnError extends boolean = false>(
  options: Options<AuthChangePasswordData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    AuthChangePasswordResponses,
    AuthChangePasswordErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/auth/web/change_password',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

export const authMe = <ThrowOnError extends boolean = false>(
  options?: Options<AuthMeData, ThrowOnError>,
) =>
  (options?.client ?? client).get<AuthMeResponses, AuthMeErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/auth/web/me',
    ...options,
  });

export const authSignin = <ThrowOnError extends boolean = false>(
  options: Options<AuthSigninData, ThrowOnError>,
) =>
  (options.client ?? client).post<AuthSigninResponses, AuthSigninErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/auth/web/signin',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

export const authSignout = <ThrowOnError extends boolean = false>(
  options?: Options<AuthSignoutData, ThrowOnError>,
) =>
  (options?.client ?? client).post<AuthSignoutResponses, unknown, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/auth/web/signout',
    ...options,
  });

export const authSignup = <ThrowOnError extends boolean = false>(
  options: Options<AuthSignupData, ThrowOnError>,
) =>
  (options.client ?? client).post<AuthSignupResponses, AuthSignupErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/auth/web/signup',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

export const authSignupStatus = <ThrowOnError extends boolean = false>(
  options?: Options<AuthSignupStatusData, ThrowOnError>,
) =>
  (options?.client ?? client).get<AuthSignupStatusResponses, unknown, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/auth/web/signup_status',
    ...options,
  });

/**
 * notify.send
 *
 * Emit a notification event through this host's installed dispatcher. The event is built from the supplied fields and fanned out per the configured routing rules. When no dispatcher is installed, returns `configured=false` with an empty result list — callers can treat that as a soft no-op.
 */
export const notifySend = <ThrowOnError extends boolean = false>(
  options: Options<NotifySendData, ThrowOnError>,
) =>
  (options.client ?? client).post<NotifySendResponses, NotifySendErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/notify.send',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * containers.list
 *
 * List containers across every registered adapter on this host. When the adapter registry is empty (no host bootstrap ran, no adapters wired in tests), [`adapters::builtin_adapters`] is consulted as a fallback so the tool is useful straight out of the box.  Sorted by `(host, name)`. Per-adapter failures land in [`ContainersListOutput::adapter_errors`]; a single bad adapter never aborts the call.
 */
export const containersList = <ThrowOnError extends boolean = false>(
  options: Options<ContainersListData, ThrowOnError>,
) =>
  (options.client ?? client).post<ContainersListResponses, ContainersListErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/containers.list',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * containers.logs
 *
 * Tail a container's logs. Routes to the owning runtime adapter (docker via the engine API, lxc via the CT's own journal over `pct exec`). `remote_ok` is the default, so this works across the pod mesh through `pod/exec`.
 */
export const containersLogs = <ThrowOnError extends boolean = false>(
  options: Options<ContainersLogsData, ThrowOnError>,
) =>
  (options.client ?? client).post<ContainersLogsResponses, ContainersLogsErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/containers.logs',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * containers.exec
 *
 * Run a one-shot command inside a container/CT and return its captured output. Routes to the owning runtime adapter (`docker exec` / `pct exec`). The building block for operator shells and the migration engine's in-guest steps.
 */
export const containersExec = <ThrowOnError extends boolean = false>(
  options: Options<ContainersExecData, ThrowOnError>,
) =>
  (options.client ?? client).post<ContainersExecResponses, ContainersExecErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/containers.exec',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * containers.reconcile_dry
 *
 * Plan-only sibling of [`containers_reconcile`]: classifies and probes (read-only), never starts and never arms the breaker.
 */
export const containersReconcileDry = <ThrowOnError extends boolean = false>(
  options: Options<ContainersReconcileDryData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    ContainersReconcileDryResponses,
    ContainersReconcileDryErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/containers.reconcile_dry',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * containers.unhold
 *
 * Clear a `Held` breaker record so the reconciler stops short- circuiting starts. Returns the cleared record's identity + new status. Errors with `NotFound` if no record matches, `NotHeld` if the record is in any state other than `Held`.
 */
export const containersUnhold = <ThrowOnError extends boolean = false>(
  options: Options<ContainersUnholdData, ThrowOnError>,
) =>
  (options.client ?? client).post<ContainersUnholdResponses, ContainersUnholdErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/containers.unhold',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * containers.unwedge
 *
 * Manually trigger recovery for a wedged container. Routes through the same [`crate::wedge::attempt_unwedge`] free fn the auto-recovery loop will call — one handler, three skins ([[feedback-cli-api-mcp-one-path]]).  Errors with `NotFound` if no adapter for `runtime` is registered, or if `(host, container_id)` doesn't resolve to a known container. The recovery-attempt outcome — including a failed recovery — comes back via [`ContainersUnwedgeOutput::recovered`] / `error`, never as an `Err`.
 */
export const containersUnwedge = <ThrowOnError extends boolean = false>(
  options: Options<ContainersUnwedgeData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    ContainersUnwedgeResponses,
    ContainersUnwedgeErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/containers.unwedge',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * containers.reconcile
 *
 * Execute one reconcile pass across every registered adapter.
 */
export const containersReconcile = <ThrowOnError extends boolean = false>(
  options: Options<ContainersReconcileData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    ContainersReconcileResponses,
    ContainersReconcileErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/containers.reconcile',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * files.read
 *
 * Read a text file. `format="llm"` strips decorative markdown; binary/multi-format reads are deferred to v2.
 */
export const filesRead = <ThrowOnError extends boolean = false>(
  options: Options<FilesReadData, ThrowOnError>,
) =>
  (options.client ?? client).post<FilesReadResponses, FilesReadErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/files.read',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * files.delete
 *
 * Combine any of: delete a file (`path`), unregister a root (`unregister_root`), remove a global ignore pattern (`remove_ignore_pattern`).
 */
export const filesDelete = <ThrowOnError extends boolean = false>(
  options: Options<FilesDeleteData, ThrowOnError>,
) =>
  (options.client ?? client).post<FilesDeleteResponses, FilesDeleteErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/files.delete',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * files.stat
 *
 * Metadata for a single path — kind (file/dir), byte size, existence flag.
 */
export const filesStat = <ThrowOnError extends boolean = false>(
  options: Options<FilesStatData, ThrowOnError>,
) =>
  (options.client ?? client).post<FilesStatResponses, FilesStatErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/files.stat',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * files.update
 *
 * Combine any of: write a file (`path` + `content`), register/update a root (`register_root_*`), add a global ignore pattern (`add_ignore_pattern`).
 */
export const filesUpdate = <ThrowOnError extends boolean = false>(
  options: Options<FilesUpdateData, ThrowOnError>,
) =>
  (options.client ?? client).post<FilesUpdateResponses, FilesUpdateErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/files.update',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * files.search
 *
 * Case-insensitive line search across one or all registered roots. Returns hits only — LLM summarisation surface dropped 2026-05-29; callers can format hits themselves.
 */
export const filesSearch = <ThrowOnError extends boolean = false>(
  options: Options<FilesSearchData, ThrowOnError>,
) =>
  (options.client ?? client).post<FilesSearchResponses, FilesSearchErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/files.search',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * files.tree
 *
 * Recursive directory tree. Compacted by default; pass `raw=true` for the unmodified filesystem layout.
 */
export const filesTree = <ThrowOnError extends boolean = false>(
  options: Options<FilesTreeData, ThrowOnError>,
) =>
  (options.client ?? client).post<FilesTreeResponses, FilesTreeErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/files.tree',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * files.list
 *
 * List filesystem resources. No args → registered roots + global ignore patterns. With `path` (and optional `root`) → directory contents at that path.
 */
export const filesList = <ThrowOnError extends boolean = false>(
  options: Options<FilesListData, ThrowOnError>,
) =>
  (options.client ?? client).post<FilesListResponses, FilesListErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/files.list',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * model.delete
 *
 * Remove a model row and its stored API key.
 */
export const modelDelete = <ThrowOnError extends boolean = false>(
  options: Options<ModelDeleteData, ThrowOnError>,
) =>
  (options.client ?? client).post<ModelDeleteResponses, ModelDeleteErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/model.delete',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * model.create
 *
 * Install a new model. Errors if `id` already exists.
 */
export const modelCreate = <ThrowOnError extends boolean = false>(
  options: Options<ModelCreateData, ThrowOnError>,
) =>
  (options.client ?? client).post<ModelCreateResponses, ModelCreateErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/model.create',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * model.list
 *
 * List installed models (filter by provider / enabled).
 */
export const modelList = <ThrowOnError extends boolean = false>(
  options: Options<ModelListData, ThrowOnError>,
) =>
  (options.client ?? client).post<ModelListResponses, ModelListErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/model.list',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * model.detail
 *
 * Show one installed model.
 */
export const modelDetail = <ThrowOnError extends boolean = false>(
  options: Options<ModelDetailData, ThrowOnError>,
) =>
  (options.client ?? client).post<ModelDetailResponses, ModelDetailErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/model.detail',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * model.update
 *
 * Modify an existing model. Errors if `id` is unknown; use `model.create` to install one.
 */
export const modelUpdate = <ThrowOnError extends boolean = false>(
  options: Options<ModelUpdateData, ThrowOnError>,
) =>
  (options.client ?? client).post<ModelUpdateResponses, ModelUpdateErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/model.update',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * model.backends_check
 *
 * Probe every configured LLM backend (DB-registered providers + the `LMSTUDIO_URL` / `OLLAMA_URL` env defaults + the Anthropic API if a key is configured) and report which are reachable and what they serve right now. Availability is dynamic, so this reflects live state at call time, not stored configuration.
 */
export const modelBackendsCheck = <ThrowOnError extends boolean = false>(
  options: Options<ModelBackendsCheckData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    ModelBackendsCheckResponses,
    ModelBackendsCheckErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/model.backends_check',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * namespace.access.create
 *
 * Grant another user access to a namespace.  `role` is `viewer` (read-only) or `collaborator` (read/write). The word "share" is intentionally avoided here because orca reserves `share.*` for filesystem-protocol shares (smb/nfs/s3). Granting another principal access to a namespace is an access-control change, not a share.
 */
export const namespaceAccessCreate = <ThrowOnError extends boolean = false>(
  options: Options<NamespaceAccessCreateData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    NamespaceAccessCreateResponses,
    NamespaceAccessCreateErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/namespace.access.create',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * namespace.list
 *
 * List all namespaces the current user can access (owned + shared).
 */
export const namespaceList = <ThrowOnError extends boolean = false>(
  options: Options<NamespaceListData, ThrowOnError>,
) =>
  (options.client ?? client).post<NamespaceListResponses, NamespaceListErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/namespace.list',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * namespace.access.delete
 *
 * Revoke a user's access to a namespace.
 */
export const namespaceAccessDelete = <ThrowOnError extends boolean = false>(
  options: Options<NamespaceAccessDeleteData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    NamespaceAccessDeleteResponses,
    NamespaceAccessDeleteErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/namespace.access.delete',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * namespace.create
 *
 * Create a new namespace owned by the current user.
 */
export const namespaceCreate = <ThrowOnError extends boolean = false>(
  options: Options<NamespaceCreateData, ThrowOnError>,
) =>
  (options.client ?? client).post<NamespaceCreateResponses, NamespaceCreateErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/namespace.create',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * namespace.access.list
 *
 * List the access grants on a namespace (owner only).
 */
export const namespaceAccessList = <ThrowOnError extends boolean = false>(
  options: Options<NamespaceAccessListData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    NamespaceAccessListResponses,
    NamespaceAccessListErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/namespace.access.list',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * namespace.use
 *
 * Set the active namespace for the current user.
 */
export const namespaceUse = <ThrowOnError extends boolean = false>(
  options: Options<NamespaceUseData, ThrowOnError>,
) =>
  (options.client ?? client).post<NamespaceUseResponses, NamespaceUseErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/namespace.use',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * namespace.delete
 *
 * Delete a namespace (owner only).
 */
export const namespaceDelete = <ThrowOnError extends boolean = false>(
  options: Options<NamespaceDeleteData, ThrowOnError>,
) =>
  (options.client ?? client).post<NamespaceDeleteResponses, NamespaceDeleteErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/namespace.delete',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * namespace.detail
 *
 * Show details of a namespace (defaults to the active one).
 */
export const namespaceDetail = <ThrowOnError extends boolean = false>(
  options: Options<NamespaceDetailData, ThrowOnError>,
) =>
  (options.client ?? client).post<NamespaceDetailResponses, NamespaceDetailErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/namespace.detail',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * agent.list
 *
 * List all available orca agents with their names and descriptions.
 */
export const agentList = <ThrowOnError extends boolean = false>(
  options: Options<AgentListData, ThrowOnError>,
) =>
  (options.client ?? client).post<AgentListResponses, AgentListErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/agent.list',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * agent.get
 *
 * Return the full system prompt for a named orca agent.
 */
export const agentGet = <ThrowOnError extends boolean = false>(
  options: Options<AgentGetData, ThrowOnError>,
) =>
  (options.client ?? client).post<AgentGetResponses, AgentGetErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/agent.get',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * agent.run
 *
 * Delegate a task to an orca agent. The model used is resolved via the `model.*` registry — per-agent pin (`agent.<name>.model_id` setting) wins, otherwise the global `is_default` model row. Prefer deterministic tools (read_doc, search_docs, list_services, etc.) over this — only use when the task genuinely needs language model reasoning.
 */
export const agentRun = <ThrowOnError extends boolean = false>(
  options: Options<AgentRunData, ThrowOnError>,
) =>
  (options.client ?? client).post<AgentRunResponses, AgentRunErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/agent.run',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * pki.create
 *
 * Initialize orca PKI (CA + server cert) or issue a plugin cert. `kind=ca` is safe to re-run; `kind=cert` requires `plugin_id`.
 */
export const pkiCreate = <ThrowOnError extends boolean = false>(
  options: Options<PkiCreateData, ThrowOnError>,
) =>
  (options.client ?? client).post<PkiCreateResponses, PkiCreateErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/pki.create',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * pki.list
 *
 * List all issued plugin certs.
 */
export const pkiList = <ThrowOnError extends boolean = false>(
  options: Options<PkiListData, ThrowOnError>,
) =>
  (options.client ?? client).post<PkiListResponses, PkiListErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/pki.list',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * secrets.update
 *
 * Update an existing secret's value/backend/metadata. Fails if no secret with this name exists — use `secrets.create` to add it or `secrets.upsert` to create-or-replace. For 'inline' backend, `value` is required; for external backends, `ref_path` is required.
 */
export const secretsUpdate = <ThrowOnError extends boolean = false>(
  options: Options<SecretsUpdateData, ThrowOnError>,
) =>
  (options.client ?? client).post<SecretsUpdateResponses, SecretsUpdateErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/secrets.update',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * secrets.detail
 *
 * [SENSITIVE] Fetch a secret value by name. Resolves via the configured backend.
 */
export const secretsDetail = <ThrowOnError extends boolean = false>(
  options: Options<SecretsDetailData, ThrowOnError>,
) =>
  (options.client ?? client).post<SecretsDetailResponses, SecretsDetailErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/secrets.detail',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * secrets.delete
 *
 * Remove a secret. The inline value is zeroed; for external backends only the orca registration is removed (the upstream vault is untouched).
 */
export const secretsDelete = <ThrowOnError extends boolean = false>(
  options: Options<SecretsDeleteData, ThrowOnError>,
) =>
  (options.client ?? client).post<SecretsDeleteResponses, SecretsDeleteErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/secrets.delete',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * secrets.list
 *
 * List configured secrets (names + backends + metadata). Never returns values.
 */
export const secretsList = <ThrowOnError extends boolean = false>(
  options: Options<SecretsListData, ThrowOnError>,
) =>
  (options.client ?? client).post<SecretsListResponses, SecretsListErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/secrets.list',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * secrets.create
 *
 * Create a new secret. Fails if a secret with this name already exists — use `secrets.update` or `secrets.upsert` to change an existing one. For 'inline' backend, `value` is required; for external backends, `ref_path` is required (e.g. 'op://Vault/Item/field'). Write the secret on a remote system with the top-level `--peer <h>` flag.
 */
export const secretsCreate = <ThrowOnError extends boolean = false>(
  options: Options<SecretsCreateData, ThrowOnError>,
) =>
  (options.client ?? client).post<SecretsCreateResponses, SecretsCreateErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/secrets.create',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * secrets.upsert
 *
 * Idempotent upsert — create the secret if absent, replace it if present. The automation-friendly write used for credential rotation. For 'inline' backend, `value` is required; for external backends, `ref_path` is required (e.g. 'op://Vault/Item/field').
 */
export const secretsUpsert = <ThrowOnError extends boolean = false>(
  options: Options<SecretsUpsertData, ThrowOnError>,
) =>
  (options.client ?? client).post<SecretsUpsertResponses, SecretsUpsertErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/secrets.upsert',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * auth.session.delete
 *
 * Remove a stored credential. `removed=false` if nothing was stored.
 */
export const authSessionDelete = <ThrowOnError extends boolean = false>(
  options: Options<AuthSessionDeleteData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    AuthSessionDeleteResponses,
    AuthSessionDeleteErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/auth.session.delete',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * auth.session.create
 *
 * Authenticate with a provider. Anthropic: pass `key`. GitHub: device-flow. Atlassian: PKCE.
 */
export const authSessionCreate = <ThrowOnError extends boolean = false>(
  options: Options<AuthSessionCreateData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    AuthSessionCreateResponses,
    AuthSessionCreateErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/auth.session.create',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * auth.session.detail
 *
 * Snapshot every configured credential the host knows about (Anthropic key + OAuth tokens).
 */
export const authSessionDetail = <ThrowOnError extends boolean = false>(
  options: Options<AuthSessionDetailData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    AuthSessionDetailResponses,
    AuthSessionDetailErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/auth.session.detail',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * auth.token.list
 *
 * List all REST/MCP bearer tokens registered on this host. Token hashes are not returned.
 */
export const authTokenList = <ThrowOnError extends boolean = false>(
  options: Options<AuthTokenListData, ThrowOnError>,
) =>
  (options.client ?? client).post<AuthTokenListResponses, AuthTokenListErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/auth.token.list',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * auth.token.create
 *
 * Mint a new REST/MCP bearer token on THIS host. Plaintext is returned exactly once and cannot be recovered from the DB. Token only authenticates calls to this host's `:12000` — not to other peers.
 */
export const authTokenCreate = <ThrowOnError extends boolean = false>(
  options: Options<AuthTokenCreateData, ThrowOnError>,
) =>
  (options.client ?? client).post<AuthTokenCreateResponses, AuthTokenCreateErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/auth.token.create',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * auth.token.delete
 *
 * Revoke a token by id. Returns `revoked=false` if the id wasn't found.
 */
export const authTokenDelete = <ThrowOnError extends boolean = false>(
  options: Options<AuthTokenDeleteData, ThrowOnError>,
) =>
  (options.client ?? client).post<AuthTokenDeleteResponses, AuthTokenDeleteErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/auth.token.delete',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * auth.login
 *
 * Authenticate the operator on THIS host and persist a CLI session at `$ORCA_HOME/session` (mode 0600). Replaces the legacy `first_admin` ambient-identity fallback on CLI + MCP-stdio. Omitting `username`/`password` is reserved for the CLI's browser flow; the daemon body refuses missing creds so an unauth'd REST caller can't trip the side effect.
 */
export const authLogin = <ThrowOnError extends boolean = false>(
  options: Options<AuthLoginData, ThrowOnError>,
) =>
  (options.client ?? client).post<AuthLoginResponses, AuthLoginErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/auth.login',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * auth.logout
 *
 * Revoke the on-disk CLI session and remove `$ORCA_HOME/session`. Idempotent — `revoked=false` means there was no active session to clear.
 */
export const authLogout = <ThrowOnError extends boolean = false>(
  options: Options<AuthLogoutData, ThrowOnError>,
) =>
  (options.client ?? client).post<AuthLogoutResponses, AuthLogoutErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/auth.logout',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * storage_mount.detail
 *
 * Detail for a single storage_mount endpoint.
 */
export const storageMountDetail = <ThrowOnError extends boolean = false>(
  options: Options<StorageMountDetailData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    StorageMountDetailResponses,
    StorageMountDetailErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/storage_mount.detail',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * storage_mount.update
 *
 * Modify an existing storage_mount endpoint. PATCH semantics — must already exist.
 */
export const storageMountUpdate = <ThrowOnError extends boolean = false>(
  options: Options<StorageMountUpdateData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    StorageMountUpdateResponses,
    StorageMountUpdateErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/storage_mount.update',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * storage_mount.delete
 *
 * Remove a registered storage_mount endpoint. Idempotent.
 */
export const storageMountDelete = <ThrowOnError extends boolean = false>(
  options: Options<StorageMountDeleteData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    StorageMountDeleteResponses,
    StorageMountDeleteErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/storage_mount.delete',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * storage_mount.create
 *
 * Register a new storage_mount endpoint. Errors if `name` is already taken.
 */
export const storageMountCreate = <ThrowOnError extends boolean = false>(
  options: Options<StorageMountCreateData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    StorageMountCreateResponses,
    StorageMountCreateErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/storage_mount.create',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * storage_mount.list
 *
 * List registered storage_mount endpoints.
 */
export const storageMountList = <ThrowOnError extends boolean = false>(
  options: Options<StorageMountListData, ThrowOnError>,
) =>
  (options.client ?? client).post<StorageMountListResponses, StorageMountListErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/storage_mount.list',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * service.backup
 *
 * Snapshot a service instance's config/data into a restorable artifact.
 */
export const serviceBackup = <ThrowOnError extends boolean = false>(
  options: Options<ServiceBackupData, ThrowOnError>,
) =>
  (options.client ?? client).post<ServiceBackupResponses, ServiceBackupErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/service.backup',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * service.status
 *
 * Health/diagnostics for a service instance.
 */
export const serviceStatus = <ThrowOnError extends boolean = false>(
  options: Options<ServiceStatusData, ThrowOnError>,
) =>
  (options.client ?? client).post<ServiceStatusResponses, ServiceStatusErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/service.status',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * service.list
 *
 * Every service backend registered with this daemon, with the runtimes and lifecycle capabilities each advertises. Empty before any service plugin loads.
 */
export const serviceList = <ThrowOnError extends boolean = false>(
  options: Options<ServiceListData, ThrowOnError>,
) =>
  (options.client ?? client).post<ServiceListResponses, ServiceListErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/service.list',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * service.configure
 *
 * Apply service-specific configuration to an instance idempotently.
 */
export const serviceConfigure = <ThrowOnError extends boolean = false>(
  options: Options<ServiceConfigureData, ThrowOnError>,
) =>
  (options.client ?? client).post<ServiceConfigureResponses, ServiceConfigureErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/service.configure',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * service.restore
 *
 * Restore a service instance from a backup artifact path.
 */
export const serviceRestore = <ThrowOnError extends boolean = false>(
  options: Options<ServiceRestoreData, ThrowOnError>,
) =>
  (options.client ?? client).post<ServiceRestoreResponses, ServiceRestoreErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/service.restore',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * service.deploy
 *
 * Build the service's `WorkloadSpec` and place it on a matching deploy target. The service backend describes *what* to run; the deploy target runs it. The runtime comes from the shared `--runtime` flag on the endpoint args.
 */
export const serviceDeploy = <ThrowOnError extends boolean = false>(
  options: Options<ServiceDeployData, ThrowOnError>,
) =>
  (options.client ?? client).post<ServiceDeployResponses, ServiceDeployErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/service.deploy',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * plugin.install
 *
 * Install a plugin. Two modes:  * `--file <path>` — **sideload**: run the full `abi_stable` + semver compat gate on the file FIRST; only on pass copy it into the install dir under a deterministic name and register its tools live (no restart). On gate failure the install is refused with the compat error and nothing is copied. * `--name <catalog-name>` — auto-download from the catalog. **Not yet implemented** (slice 5); returns a clear error.
 */
export const pluginInstall = <ThrowOnError extends boolean = false>(
  options: Options<PluginInstallData, ThrowOnError>,
) =>
  (options.client ?? client).post<PluginInstallResponses, PluginInstallErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/plugin.install',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * plugin.uninstall
 *
 * Remove a plugin: delete its cdylib from the install dir and unregister its tools from the live registry. Idempotent — reports what it actually removed.
 */
export const pluginUninstall = <ThrowOnError extends boolean = false>(
  options: Options<PluginUninstallData, ThrowOnError>,
) =>
  (options.client ?? client).post<PluginUninstallResponses, PluginUninstallErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/plugin.uninstall',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * plugin.list
 *
 * plugin_list
 */
export const pluginList = <ThrowOnError extends boolean = false>(
  options: Options<PluginListData, ThrowOnError>,
) =>
  (options.client ?? client).post<PluginListResponses, PluginListErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/plugin.list',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * system.detail_view
 *
 * Chart-ready typed series for the host detail page. The daemon on the target peer reads its local history ring, projects each metric into SVG-space using the caller's `width`/`height`, and emits a flat, natively-renderable shape.
 */
export const systemDetailView = <ThrowOnError extends boolean = false>(
  options: Options<SystemDetailViewData, ThrowOnError>,
) =>
  (options.client ?? client).post<SystemDetailViewResponses, SystemDetailViewErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/system.detail_view',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * storage.mount
 *
 * Render every enabled network-share entry in the `managed_mounts` store into the orca autofs direct map and reload autofs. autofs then owns on-demand mounting, idle unmount, and ordered-source (primary → failover) failover; the `storage.recover_stale` loop covers the one case autofs can't self-heal (an actively-held stale hard mount). Idempotent — a run that changes nothing neither rewrites files nor reloads autofs.
 */
export const storageMount = <ThrowOnError extends boolean = false>(
  options: Options<StorageMountData, ThrowOnError>,
) =>
  (options.client ?? client).post<StorageMountResponses, StorageMountErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/storage.mount',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * storage.unmount
 *
 * Unmount a target on a named backend. Errors if the provider is unknown or does not advertise the `unmount` capability.
 */
export const storageUnmount = <ThrowOnError extends boolean = false>(
  options: Options<StorageUnmountData, ThrowOnError>,
) =>
  (options.client ?? client).post<StorageUnmountResponses, StorageUnmountErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/storage.unmount',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * storage.shares
 *
 * Enumerate shares/volumes across registered backends. Backends that don't advertise `list` are skipped; per-backend failures are collected into `errors` rather than failing the whole call.
 */
export const storageShares = <ThrowOnError extends boolean = false>(
  options: Options<StorageSharesData, ThrowOnError>,
) =>
  (options.client ?? client).post<StorageSharesResponses, StorageSharesErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/storage.shares',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * storage.recover
 *
 * Self-heal stale autofs mounts across the declared network shares — the one failure mode autofs can't recover itself (an actively-held stale `hard` mount). Probes each declared target; a stale one is force-released and re-accessed so autofs remounts + fails over to the next ordered source. This is what the periodic self-heal schedule invokes per host.
 */
export const storageRecover = <ThrowOnError extends boolean = false>(
  options: Options<StorageRecoverData, ThrowOnError>,
) =>
  (options.client ?? client).post<StorageRecoverResponses, StorageRecoverErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/storage.recover',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * storage.list
 *
 * Every storage backend registered with this daemon, with the capabilities each advertises. Empty before any storage adapter has bootstrapped.
 */
export const storageList = <ThrowOnError extends boolean = false>(
  options: Options<StorageListData, ThrowOnError>,
) =>
  (options.client ?? client).post<StorageListResponses, StorageListErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/storage.list',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * config.upsert
 *
 * Upsert a config row. Refuses to write rows owned by a different host — cross-host writes route via the pod mesh once peer-tool dispatch lands (§3.3).
 */
export const configUpsert = <ThrowOnError extends boolean = false>(
  options: Options<ConfigUpsertData, ThrowOnError>,
) =>
  (options.client ?? client).post<ConfigUpsertResponses, ConfigUpsertErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/config.upsert',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * config.delete
 *
 * Delete a config row owned by the local host.
 */
export const configDelete = <ThrowOnError extends boolean = false>(
  options: Options<ConfigDeleteData, ThrowOnError>,
) =>
  (options.client ?? client).post<ConfigDeleteResponses, ConfigDeleteErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/config.delete',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * config.detail
 *
 * Fetch a single config row by noun+name.
 */
export const configDetail = <ThrowOnError extends boolean = false>(
  options: Options<ConfigDetailData, ThrowOnError>,
) =>
  (options.client ?? client).post<ConfigDetailResponses, ConfigDetailErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/config.detail',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * config.list
 *
 * List config rows. Optionally filter by noun and/or host_owner.
 */
export const configList = <ThrowOnError extends boolean = false>(
  options: Options<ConfigListData, ThrowOnError>,
) =>
  (options.client ?? client).post<ConfigListResponses, ConfigListErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/config.list',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * system.update
 *
 * The single system-update tool. Covers orca binary updates, host identity (hostname/fqdn/addressing), and OS package upgrades. Omit every arg for a read-only state probe.
 */
export const systemUpdate = <ThrowOnError extends boolean = false>(
  options: Options<SystemUpdateData, ThrowOnError>,
) =>
  (options.client ?? client).post<SystemUpdateResponses, SystemUpdateErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/system.update',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * system.install
 *
 * Install orca on this host. Always wires the user-level install (binary, ~/.claude symlinks, MCP registration, PKI). When `service_user` is set, also bootstraps a system service user with SSH access — replaces the former separate `system.bootstrap` tool.
 */
export const systemInstall = <ThrowOnError extends boolean = false>(
  options: Options<SystemInstallData, ThrowOnError>,
) =>
  (options.client ?? client).post<SystemInstallResponses, SystemInstallErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/system.install',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * system.delete
 *
 * Uninstall orca from this host: remove binary, MCP registration, CLAUDE.md symlinks, AND the daemon supervisor unit (launchd / systemd / openrc / unraid). Absorbed the former `system.daemon.uninstall`.
 */
export const systemDelete = <ThrowOnError extends boolean = false>(
  options: Options<SystemDeleteData, ThrowOnError>,
) =>
  (options.client ?? client).post<SystemDeleteResponses, SystemDeleteErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/system.delete',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * system.serve_release
 *
 * Serve a release asset from GitHub on behalf of a peer that lacks the `github_token` secret. Resolves the token locally, downloads the asset for the requested `target`, verifies sha256 against the release checksum blob, and returns the bytes base64-encoded.
 */
export const systemServeRelease = <ThrowOnError extends boolean = false>(
  options: Options<SystemServeReleaseData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    SystemServeReleaseResponses,
    SystemServeReleaseErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/system.serve_release',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * system.capability_enable
 *
 * Clear a `Disabled` row and immediately re-probe. Returned row reflects whichever state the live probe lands in (`Available` or `Absent`). No-op-like when the row wasn't Disabled — still re-probes so the returned state is fresh.
 */
export const systemCapabilityEnable = <ThrowOnError extends boolean = false>(
  options: Options<SystemCapabilityEnableData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    SystemCapabilityEnableResponses,
    SystemCapabilityEnableErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/system.capability_enable',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * system.capability_disable
 *
 * Mark a provider `Disabled`. Sticky across daemon restarts — `probe_all_capabilities` leaves Disabled rows alone. Idempotent.
 */
export const systemCapabilityDisable = <ThrowOnError extends boolean = false>(
  options: Options<SystemCapabilityDisableData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    SystemCapabilityDisableResponses,
    SystemCapabilityDisableErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/system.capability_disable',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * system.capability_list
 *
 * Every provider this host has ever probed or had set by an operator. Returns an empty list before the first daemon startup probe runs.
 */
export const systemCapabilityList = <ThrowOnError extends boolean = false>(
  options: Options<SystemCapabilityListData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    SystemCapabilityListResponses,
    SystemCapabilityListErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/system.capability_list',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * system.capability_recheck
 *
 * Force a fresh probe of one provider. Persists + returns the new state. No-op for Disabled rows (operator intent wins).
 */
export const systemCapabilityRecheck = <ThrowOnError extends boolean = false>(
  options: Options<SystemCapabilityRecheckData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    SystemCapabilityRecheckResponses,
    SystemCapabilityRecheckErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/system.capability_recheck',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * system.kill
 *
 * Kill stale orca runtime processes (mcp-serve, daemon start) so a binary swap is picked up by their clients on next call. Safe to run before any deploy; no-op when nothing matches.
 */
export const systemKill = <ThrowOnError extends boolean = false>(
  options: Options<SystemKillData, ThrowOnError>,
) =>
  (options.client ?? client).post<SystemKillResponses, SystemKillErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/system.kill',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * db.stats
 *
 * Per-table storage cost (bytes + row count). Backed by the SQLite `dbstat` virtual table — compiled in via `SQLITE_ENABLE_DBSTAT_VTAB`. Use to find which table is responsible for db file growth.
 */
export const dbStats = <ThrowOnError extends boolean = false>(
  options: Options<DbStatsData, ThrowOnError>,
) =>
  (options.client ?? client).post<DbStatsResponses, DbStatsErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/db.stats',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * db.update
 *
 * Drive the migration runner. `action`: - `migrate`: apply all pending migrations. - `up`: apply the next pending migration (one step). - `down`: revert the most recently applied migration (one step).
 */
export const dbUpdate = <ThrowOnError extends boolean = false>(
  options: Options<DbUpdateData, ThrowOnError>,
) =>
  (options.client ?? client).post<DbUpdateResponses, DbUpdateErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/db.update',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * db.detail
 *
 * Show current schema version and pending-migration count.
 */
export const dbDetail = <ThrowOnError extends boolean = false>(
  options: Options<DbDetailData, ThrowOnError>,
) =>
  (options.client ?? client).post<DbDetailResponses, DbDetailErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/db.detail',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * db.sweep
 *
 * Delete rows older than `days` from `table`. FTS5 mirrors cascade via existing triggers. Run `db.compact` afterwards (or wait for incremental_vacuum) to actually reclaim disk space.
 */
export const dbSweep = <ThrowOnError extends boolean = false>(
  options: Options<DbSweepData, ThrowOnError>,
) =>
  (options.client ?? client).post<DbSweepResponses, DbSweepErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/db.sweep',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * db.compact
 *
 * Reclaim disk space. Default = full `VACUUM` (acquires write lock for the duration; needs ~2× db size in temp disk). Pass `incremental=true` for a cheap incremental pass.  First-time use note: a full VACUUM is also required ONCE on an existing database to activate `auto_vacuum=INCREMENTAL`. After that the incremental path can keep the file compact without locking.
 */
export const dbCompact = <ThrowOnError extends boolean = false>(
  options: Options<DbCompactData, ThrowOnError>,
) =>
  (options.client ?? client).post<DbCompactResponses, DbCompactErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/db.compact',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * system.build
 *
 * Build a distributable package (deb/rpm/apk/PKGBUILD/pkg/homebrew) from the current orca binary. Format auto-detected from host OS when not provided. Postinst scripts delegate to `system install --service-user orca` (which absorbed the old `system.bootstrap` + supervisor-install responsibilities).
 */
export const systemBuild = <ThrowOnError extends boolean = false>(
  options: Options<SystemBuildData, ThrowOnError>,
) =>
  (options.client ?? client).post<SystemBuildResponses, SystemBuildErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/system.build',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * schedule.status
 *
 * Show per-job last-run status from the scheduler_runs history.
 */
export const scheduleStatus = <ThrowOnError extends boolean = false>(
  options: Options<ScheduleStatusData, ThrowOnError>,
) =>
  (options.client ?? client).post<ScheduleStatusResponses, ScheduleStatusErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/schedule.status',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * schedule.run
 *
 * Invoke a scheduled job immediately, out-of-band from the loop. Useful for testing schedule wiring without waiting for the next firing.
 */
export const scheduleRun = <ThrowOnError extends boolean = false>(
  options: Options<ScheduleRunData, ThrowOnError>,
) =>
  (options.client ?? client).post<ScheduleRunResponses, ScheduleRunErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/schedule.run',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * schedule.list
 *
 * List schedule rows with their next firing time.
 */
export const scheduleList = <ThrowOnError extends boolean = false>(
  options: Options<ScheduleListData, ThrowOnError>,
) =>
  (options.client ?? client).post<ScheduleListResponses, ScheduleListErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/schedule.list',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * system.detail
 *
 * Snapshot of orca's installation: binary, ~/.claude/CLAUDE.md, vault dir, agents symlink, PKI init, MCP registration.
 */
export const systemDetail = <ThrowOnError extends boolean = false>(
  options: Options<SystemDetailData, ThrowOnError>,
) =>
  (options.client ?? client).post<SystemDetailResponses, SystemDetailErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/system.detail',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * system.retention_list
 *
 * Resolved retention for every peer + the global default. UI uses this to render the per-system retention controls.
 */
export const systemRetentionList = <ThrowOnError extends boolean = false>(
  options: Options<SystemRetentionListData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    SystemRetentionListResponses,
    SystemRetentionListErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/system.retention_list',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * system.retention_set
 *
 * Set one or more retention knobs for a peer (or the global default). Returns the resolved policy after the write.
 */
export const systemRetentionSet = <ThrowOnError extends boolean = false>(
  options: Options<SystemRetentionSetData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    SystemRetentionSetResponses,
    SystemRetentionSetErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/system.retention_set',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * system.retention_get
 *
 * Resolve the effective retention policy for one peer (or the global default if `peer` is omitted). Returns the same shape `set` accepts.
 */
export const systemRetentionGet = <ThrowOnError extends boolean = false>(
  options: Options<SystemRetentionGetData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    SystemRetentionGetResponses,
    SystemRetentionGetErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/system.retention_get',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * pod.history
 *
 * Snapshot history for one peer, newest-first. Both the UI (timeseries) and the sync puller (watermarked pull) use this. Latest-per-peer is already on `pod.list` (each member row enriches its `system` field from the same `host_status` table), so no separate list verb exists.
 */
export const podHistory = <ThrowOnError extends boolean = false>(
  options: Options<PodHistoryData, ThrowOnError>,
) =>
  (options.client ?? client).post<PodHistoryResponses, PodHistoryErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/pod.history',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * pod.update
 *
 * Update pod-level settings on this host or — when a peer target is set (`--peer <h>` / `X-Orca-Peer` / MCP `peer` arg) — on the named remote peer over the pod mesh via the universal peer-dispatch stanza. Currently exposes `self_secure` (Tier-2 secrets-storage permission). Admin-only because flipping it can authorize secrets replication into this host.
 */
export const podUpdate = <ThrowOnError extends boolean = false>(
  options: Options<PodUpdateData, ThrowOnError>,
) =>
  (options.client ?? client).post<PodUpdateResponses, PodUpdateErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/pod.update',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * pod.kick
 *
 * Evict a paired peer: best-effort notify, then drop `pod_peers` + `pod_trust` rows for it. Mirrors today's `system.peer.delete` semantics.
 */
export const podKick = <ThrowOnError extends boolean = false>(
  options: Options<PodKickData, ThrowOnError>,
) =>
  (options.client ?? client).post<PodKickResponses, PodKickErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/pod.kick',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * pod.join
 *
 * Initiate or complete a pod-membership pairing.  `action`: - `"invite"` — inviter pushes an offer to a discovered joiner. Requires `addr` (joiner's host or host:port from mDNS discovery). Returns a pairing code to show the operator; the joiner auto-accepts if its daemon received the code in-band. - `"join"` — joiner requests an offer from an inviter not yet in mDNS. Requires `addr` (inviter's host or host:port). Returns the code the inviter will display. - `"accept"` — joiner accepts a pending inbound offer by its 6-char code. Requires `code`. Returns the inviter identity after join.
 */
export const podJoin = <ThrowOnError extends boolean = false>(
  options: Options<PodJoinData, ThrowOnError>,
) =>
  (options.client ?? client).post<PodJoinResponses, PodJoinErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/pod.join',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * pod.trust
 *
 * Set trust for a paired peer. Without `push`, mutates OUR local trust (`local_secure`). With `push: true`, executes on the remote peer over mTLS so THEY trust US (`peer_secure` from our perspective).
 */
export const podTrust = <ThrowOnError extends boolean = false>(
  options: Options<PodTrustData, ThrowOnError>,
) =>
  (options.client ?? client).post<PodTrustResponses, PodTrustErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/pod.trust',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * pod.recover
 *
 * Clear a stale `departed_at` flag for a peer on THIS host. Recovery tool for the 2026-05-28 kick/peer-leaving bug (and any future false-depart). No network call — purely local row repair.
 */
export const podRecover = <ThrowOnError extends boolean = false>(
  options: Options<PodRecoverData, ThrowOnError>,
) =>
  (options.client ?? client).post<PodRecoverResponses, PodRecoverErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/pod.recover',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * pod.ping
 *
 * mTLS ping a paired peer; returns latency + their self-reported identity. Kept distinct from `system.detail --peer <id>` because ping latency is a *relationship* measurement between this host and the peer, not a property of the peer itself.
 */
export const podPing = <ThrowOnError extends boolean = false>(
  options: Options<PodPingData, ThrowOnError>,
) =>
  (options.client ?? client).post<PodPingResponses, PodPingErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/pod.ping',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * pod.snapshot
 *
 * Pre-classified rollup of pod state for the systems UI. Same `members` payload as `pod.list`, plus candidate / stale / inbound-offer classification and cluster-membership matching computed server-side so every surface gets one shaped response instead of re-implementing the rules per client.
 */
export const podSnapshot = <ThrowOnError extends boolean = false>(
  options: Options<PodSnapshotData, ThrowOnError>,
) =>
  (options.client ?? client).post<PodSnapshotResponses, PodSnapshotErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/pod.snapshot',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * pod.cancel_offer
 *
 * Clear stuck outbound pairing offer(s) for an address. Use when a previous +Add never got accepted/expired and is blocking new invites. Idempotent — returns rows removed (0 if nothing matched).
 */
export const podCancelOffer = <ThrowOnError extends boolean = false>(
  options: Options<PodCancelOfferData, ThrowOnError>,
) =>
  (options.client ?? client).post<PodCancelOfferResponses, PodCancelOfferErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/pod.cancel_offer',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * pod.forget
 *
 * Forget a stale/orphan peer_id mesh-wide: hard-delete it here AND fan a one-way notice to every live member so they drop it too. Use for orphans left by machine_id churn or decommissioned hosts — NOT for evicting a live peer (that's `pod kick`).
 */
export const podForget = <ThrowOnError extends boolean = false>(
  options: Options<PodForgetData, ThrowOnError>,
) =>
  (options.client ?? client).post<PodForgetResponses, PodForgetErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/pod.forget',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * pod.instances
 *
 * Fully-shaped instance roster for the systems UI. One round-trip returns the local synthetic row + every active joined peer projected into `PodInstance` (snake_case fields, server-derived `reachable_addrs`), alongside the same candidate / stale / inbound-offer classification `pod.snapshot` produces. Replaces the client-side seed + bucket logic in `peers.svelte.ts` (slice S3).
 */
export const podInstances = <ThrowOnError extends boolean = false>(
  options: Options<PodInstancesData, ThrowOnError>,
) =>
  (options.client ?? client).post<PodInstancesResponses, PodInstancesErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/pod.instances',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * pod.sync
 *
 * Force a one-shot replication tick on this host (or — with `peer_id` set — on the named remote peer via the universal peer-dispatch path) and return a per-source-peer report. Replaces "wait 60s for the background tick to fire and hope it worked." `peer` arg optionally filters which source peer we pull from (hostname / peer_id / addr) — omit to pull from every paired peer. Admin: this is operator-facing and can surface mesh errors.
 */
export const podSync = <ThrowOnError extends boolean = false>(
  options: Options<PodSyncData, ThrowOnError>,
) =>
  (options.client ?? client).post<PodSyncResponses, PodSyncErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/pod.sync',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * pod.leave
 *
 * Voluntary pod exit: notify every paired peer we're leaving (best-effort), then drop all `pod_peers` + `pod_trust` rows on this host. PKI material is left in place — call `system bootstrap` to fully reset.
 */
export const podLeave = <ThrowOnError extends boolean = false>(
  options: Options<PodLeaveData, ThrowOnError>,
) =>
  (options.client ?? client).post<PodLeaveResponses, PodLeaveErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/pod.leave',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * pod.detail
 *
 * Days-remaining + rotation state for every mesh cert on this host, plus the current `self_secure` (Tier-2 secrets-storage) setting.
 */
export const podDetail = <ThrowOnError extends boolean = false>(
  options: Options<PodDetailData, ThrowOnError>,
) =>
  (options.client ?? client).post<PodDetailResponses, PodDetailErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/pod.detail',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * pod.list
 *
 * Unified pod-membership view: joined members + in-flight handshakes + mDNS-discovered candidates, each row tagged by `state`. Replaces the trio of `system.peer.list`, `system.peer.discovery.list`, and `system.peer.handshake.list` (2026-05-28 consolidation).
 */
export const podList = <ThrowOnError extends boolean = false>(
  options: Options<PodListData, ThrowOnError>,
) =>
  (options.client ?? client).post<PodListResponses, PodListErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/pod.list',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * inventory.tree
 *
 * Unified systems-view inventory tree. Returns clusters of recursive `InventoryNode` trees (parent-inferred via MAC claims, with `system.parent_peer_id` overrides). The UI renders each node as a card that visually contains its children.
 */
export const inventoryTree = <ThrowOnError extends boolean = false>(
  options: Options<InventoryTreeData, ThrowOnError>,
) =>
  (options.client ?? client).post<InventoryTreeResponses, InventoryTreeErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/inventory.tree',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * network.topology_view
 *
 * Network topology graph for the systems view. Returns a flat node + edge list (NOT a nested tree) suitable for force-directed canvas rendering. Nodes are peers; edges are parent-inference relationships (MAC-claim or explicit `parent_peer_id` overrides). Clusters surface as compound nodes when `ClusterRoster` populates them.
 */
export const networkTopologyView = <ThrowOnError extends boolean = false>(
  options: Options<NetworkTopologyViewData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    NetworkTopologyViewResponses,
    NetworkTopologyViewErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/network.topology_view',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * plugin.detail
 *
 * plugin_detail
 */
export const pluginDetail = <ThrowOnError extends boolean = false>(
  options: Options<PluginDetailData, ThrowOnError>,
) =>
  (options.client ?? client).post<PluginDetailResponses, PluginDetailErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/plugin.detail',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * plugin.create
 *
 * Install a plugin from a manifest. Errors if the resolved id already exists — use `plugin.update` to modify an already-installed plugin.
 */
export const pluginCreate = <ThrowOnError extends boolean = false>(
  options: Options<PluginCreateData, ThrowOnError>,
) =>
  (options.client ?? client).post<PluginCreateResponses, PluginCreateErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/plugin.create',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * plugin.delete
 *
 * Delete the whole plugin, or just a single credential or data entry. Errors if `id` is not registered.
 */
export const pluginDelete = <ThrowOnError extends boolean = false>(
  options: Options<PluginDeleteData, ThrowOnError>,
) =>
  (options.client ?? client).post<PluginDeleteResponses, PluginDeleteErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/plugin.delete',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * plugin.update
 *
 * Modify an existing plugin: toggle enabled, set/sync credentials, set data. Errors if `id` is not a registered plugin — use `plugin.create` to install.
 */
export const pluginUpdate = <ThrowOnError extends boolean = false>(
  options: Options<PluginUpdateData, ThrowOnError>,
) =>
  (options.client ?? client).post<PluginUpdateResponses, PluginUpdateErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/plugin.update',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * schema.delete
 *
 * Remove a schema database from orca.db by name.
 */
export const schemaDelete = <ThrowOnError extends boolean = false>(
  options: Options<SchemaDeleteData, ThrowOnError>,
) =>
  (options.client ?? client).post<SchemaDeleteResponses, SchemaDeleteErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/schema.delete',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * schema.create
 *
 * Add or update a schema database in orca.db. Use container OR host/port, not both.
 */
export const schemaCreate = <ThrowOnError extends boolean = false>(
  options: Options<SchemaCreateData, ThrowOnError>,
) =>
  (options.client ?? client).post<SchemaCreateResponses, SchemaCreateErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/schema.create',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * schema.detail
 *
 * Multi-tab introspection across every configured database. Result is `{ tabs, showTabs, errors?, domains }` — full schema view including the flattened domain list that the old `schema.view.list` returned separately.
 */
export const schemaDetail = <ThrowOnError extends boolean = false>(
  options: Options<SchemaDetailData, ThrowOnError>,
) =>
  (options.client ?? client).post<SchemaDetailResponses, SchemaDetailErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/schema.detail',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * schema.list
 *
 * list_schemas
 */
export const schemaList = <ThrowOnError extends boolean = false>(
  options: Options<SchemaListData, ThrowOnError>,
) =>
  (options.client ?? client).post<SchemaListResponses, SchemaListErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/schema.list',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * spec.list
 *
 * List every registered OpenAPI / GraphQL spec — filesystem-resident, DB-backed, and plugin-declared — with per-source metadata.
 */
export const specList = <ThrowOnError extends boolean = false>(
  options: Options<SpecListData, ThrowOnError>,
) =>
  (options.client ?? client).post<SpecListResponses, SpecListErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/spec.list',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * spec.create
 *
 * Fetch a JSON OpenAPI spec from `url` and persist it under `name` in orca.db.
 */
export const specCreate = <ThrowOnError extends boolean = false>(
  options: Options<SpecCreateData, ThrowOnError>,
) =>
  (options.client ?? client).post<SpecCreateResponses, SpecCreateErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/spec.create',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * spec.graphql.update
 *
 * Proxy a GraphQL request to a Shopify shop using the configured shop+token. Returns the raw upstream JSON body.
 */
export const specGraphqlUpdate = <ThrowOnError extends boolean = false>(
  options: Options<SpecGraphqlUpdateData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    SpecGraphqlUpdateResponses,
    SpecGraphqlUpdateErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/spec.graphql.update',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * spec.refresh
 *
 * Re-fetch a previously-registered spec from its stored URL and update orca.db.
 */
export const specRefresh = <ThrowOnError extends boolean = false>(
  options: Options<SpecRefreshData, ThrowOnError>,
) =>
  (options.client ?? client).post<SpecRefreshResponses, SpecRefreshErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/spec.refresh',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * spec.sync-mcp
 *
 * Connect to `server` (an MCP server), call its `{prefix}_spec_list` and `{prefix}_spec_schema` tools, and upsert every advertised repo into orca.db.
 */
export const specSyncMcp = <ThrowOnError extends boolean = false>(
  options: Options<SpecSyncMcpData, ThrowOnError>,
) =>
  (options.client ?? client).post<SpecSyncMcpResponses, SpecSyncMcpErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/spec.sync-mcp',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * spec.graphql.detail
 *
 * Parse the local `<repo>.graphql` SDL into a structured types/queries/mutations view.
 */
export const specGraphqlDetail = <ThrowOnError extends boolean = false>(
  options: Options<SpecGraphqlDetailData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    SpecGraphqlDetailResponses,
    SpecGraphqlDetailErrors,
    ThrowOnError
  >({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/spec.graphql.detail',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * spec.delete
 *
 * Remove a spec from orca.db. Returns `removed: true` when a row was deleted.
 */
export const specDelete = <ThrowOnError extends boolean = false>(
  options: Options<SpecDeleteData, ThrowOnError>,
) =>
  (options.client ?? client).post<SpecDeleteResponses, SpecDeleteErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/spec.delete',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * spec.detail
 *
 * Dump orca's own OpenAPI JSON document. Used by build pipelines that don't want to spin up the HTTP server.
 */
export const specDetail = <ThrowOnError extends boolean = false>(
  options: Options<SpecDetailData, ThrowOnError>,
) =>
  (options.client ?? client).post<SpecDetailResponses, SpecDetailErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/spec.detail',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * vm.list
 *
 * List vm units (search/filter via query)
 */
export const vmList = <ThrowOnError extends boolean = false>(
  options: Options<VmListData, ThrowOnError>,
) =>
  (options.client ?? client).post<VmListResponses, VmListErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/vm.list',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * vm.detail
 *
 * Inspect one vm unit
 */
export const vmDetail = <ThrowOnError extends boolean = false>(
  options: Options<VmDetailData, ThrowOnError>,
) =>
  (options.client ?? client).post<VmDetailResponses, VmDetailErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/vm.detail',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * vm.start
 *
 * Update (start) a vm unit
 */
export const vmStart = <ThrowOnError extends boolean = false>(
  options: Options<VmStartData, ThrowOnError>,
) =>
  (options.client ?? client).post<VmStartResponses, VmStartErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/vm.start',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * vm.stop
 *
 * Update (stop) a vm unit
 */
export const vmStop = <ThrowOnError extends boolean = false>(
  options: Options<VmStopData, ThrowOnError>,
) =>
  (options.client ?? client).post<VmStopResponses, VmStopErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/vm.stop',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * vm.shutdown
 *
 * Update (shutdown) a vm unit
 */
export const vmShutdown = <ThrowOnError extends boolean = false>(
  options: Options<VmShutdownData, ThrowOnError>,
) =>
  (options.client ?? client).post<VmShutdownResponses, VmShutdownErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/vm.shutdown',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * vm.reboot
 *
 * Update (reboot) a vm unit
 */
export const vmReboot = <ThrowOnError extends boolean = false>(
  options: Options<VmRebootData, ThrowOnError>,
) =>
  (options.client ?? client).post<VmRebootResponses, VmRebootErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/vm.reboot',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * vm.provision
 *
 * Create (provision) on a vm
 */
export const vmProvision = <ThrowOnError extends boolean = false>(
  options: Options<VmProvisionData, ThrowOnError>,
) =>
  (options.client ?? client).post<VmProvisionResponses, VmProvisionErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/vm.provision',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * vm.delete
 *
 * Delete a vm unit
 */
export const vmDelete = <ThrowOnError extends boolean = false>(
  options: Options<VmDeleteData, ThrowOnError>,
) =>
  (options.client ?? client).post<VmDeleteResponses, VmDeleteErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/vm.delete',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * lxc.list
 *
 * List lxc units (search/filter via query)
 */
export const lxcList = <ThrowOnError extends boolean = false>(
  options: Options<LxcListData, ThrowOnError>,
) =>
  (options.client ?? client).post<LxcListResponses, LxcListErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/lxc.list',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * lxc.detail
 *
 * Inspect one lxc unit
 */
export const lxcDetail = <ThrowOnError extends boolean = false>(
  options: Options<LxcDetailData, ThrowOnError>,
) =>
  (options.client ?? client).post<LxcDetailResponses, LxcDetailErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/lxc.detail',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * lxc.start
 *
 * Update (start) a lxc unit
 */
export const lxcStart = <ThrowOnError extends boolean = false>(
  options: Options<LxcStartData, ThrowOnError>,
) =>
  (options.client ?? client).post<LxcStartResponses, LxcStartErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/lxc.start',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * lxc.stop
 *
 * Update (stop) a lxc unit
 */
export const lxcStop = <ThrowOnError extends boolean = false>(
  options: Options<LxcStopData, ThrowOnError>,
) =>
  (options.client ?? client).post<LxcStopResponses, LxcStopErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/lxc.stop',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * lxc.shutdown
 *
 * Update (shutdown) a lxc unit
 */
export const lxcShutdown = <ThrowOnError extends boolean = false>(
  options: Options<LxcShutdownData, ThrowOnError>,
) =>
  (options.client ?? client).post<LxcShutdownResponses, LxcShutdownErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/lxc.shutdown',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * lxc.reboot
 *
 * Update (reboot) a lxc unit
 */
export const lxcReboot = <ThrowOnError extends boolean = false>(
  options: Options<LxcRebootData, ThrowOnError>,
) =>
  (options.client ?? client).post<LxcRebootResponses, LxcRebootErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/lxc.reboot',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * lxc.provision
 *
 * Create (provision) on a lxc
 */
export const lxcProvision = <ThrowOnError extends boolean = false>(
  options: Options<LxcProvisionData, ThrowOnError>,
) =>
  (options.client ?? client).post<LxcProvisionResponses, LxcProvisionErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/lxc.provision',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * lxc.delete
 *
 * Delete a lxc unit
 */
export const lxcDelete = <ThrowOnError extends boolean = false>(
  options: Options<LxcDeleteData, ThrowOnError>,
) =>
  (options.client ?? client).post<LxcDeleteResponses, LxcDeleteErrors, ThrowOnError>({
    security: [
      {
        in: 'cookie',
        name: 'orca_session',
        type: 'apiKey',
      },
    ],
    url: '/api/v1/lxc.delete',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });
